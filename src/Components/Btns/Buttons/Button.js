import Btns from "../Btns";
import "./Button.css";
const Button = ({ title }) => {
  const Btns = () => {
    alert("not found D:");
  };
  return (
    <button id="btn" onClick={Btns}>
      {title}
    </button>
  );
};

export default Button;
